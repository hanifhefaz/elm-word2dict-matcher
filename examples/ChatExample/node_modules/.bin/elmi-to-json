#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elmi-to-json/bin/elmi-to-json" "$@"
  ret=$?
else 
  node  "$basedir/../elmi-to-json/bin/elmi-to-json" "$@"
  ret=$?
fi
exit $ret
